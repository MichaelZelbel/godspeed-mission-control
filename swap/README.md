# Try another assistant

Start with three-questions.md. Ask the assistant to verify the current OpenCode installation and configure your chosen provider. The JSON examples here set conservative permissions; they deliberately do not select a model. Choose a currently available model during setup. Add the notebook only after the local file and skill tests pass.
