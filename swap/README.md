# Try another assistant (Chapter 33)

Start with `opencode.json`, which has no notebook. It names the historical test model; check current availability and price before use. Inspect effective settings because a client can merge project and user configuration.

- `three-questions.md`: the complete current exercise, including masked key input, local file tests and limits.
- `openrouter-notes.md`: provider setup, credential handling and cost checks without a fixed price promise.
- `opencode-with-notebook.json`: optional complete configuration, only after Chapter 29. It names `MENERIO_API_KEY` without storing its value.

Copy only the selected configuration beside your hub's `AGENTS.md`, preserving any existing configuration. File access, skill selection, execution and scheduling are separate tests. No schedule is migrated by these JSON files.
